server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/compassed_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: compassed
    password: 1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

openai:
  api:
    key: ${OPENAI_API_KEY}
  model: gpt-4.1-mini

