## Quy ước làm việc
- Làm xong task nào ở trên thì di chuyển xuống thứ tự tiếp theo ở phần task done
- Task nào bạn duyệt xong thì chuyển xuống mục `Task Done` với dấu ✅.

# TASKS (sắp xếp lại theo dependency tối ưu)
# Không còn task mở

# TASK DONE

✅ 24. Phần render roadmap hiện tại:
   đã quay lại dùng API AI để render roadmap cá nhân hóa theo profile user (`academicTrack`, level, placement...);
   trong màn học roadmap, mỗi lesson có thể mở giáo trình AI riêng theo module/lesson của user;
   khi user nhấn `Bắt đầu`, hệ thống gọi endpoint AI lesson và hiển thị nội dung bài học ngay trong roadmap.

✅ 25. Phần import đề json bên admin:
   khi import môn Toán, hệ thống tự normalize ký hiệu mũ/chỉ số sang LaTeX hợp lệ;
   ví dụ `y=-x^3+3x` sẽ được lưu thành `y=-x^{3}+3x`;
   đã thêm test backend cho case import legacy JSON môn Toán.

✅ 1. Thêm phần chọn lớp:
   check user -> chưa phân loại lớp -> login/register vào homepage -> yêu cầu chọn lớp (11-12-ôn thi đại học).

✅ 2. Check user login với Google account:
   new user -> hỏi làm placement test giống user đăng ký mới;
   old user -> giữ flow bình thường.

✅ 3. User login -> check placement_attempt status:
   chưa có hoặc IN_PROGRESS -> header trang home hiện button Bài kiểm tra đầu vào (Placement Test, lưu tiến trình của user);
   SUBMITTED -> flow bình thường.

✅ 4. Bắt buộc user muốn học theo roadmap phải làm placement test:
   dùng kết quả level để phân loại roadmap.

✅ 5. Validate button Join Program ở trang Home:
   chưa đăng ký -> chuyển trang checkout;
   đã đăng ký -> trạng thái tích xanh, không mapping/action.

✅ 6. Mapping nút How it works ở header:
   scroll xuống section How it works ở trang Home.

✅ 7. Mapping nút Pricing:
   chuyển sang trang Checkout.

✅ 8. Mapping nút Learning -> dropdown:
   Làm đề;
   Roadmap Tutor;
   Học cùng AI.

✅ 9. Làm đề:
   cho phép user chọn đề theo Level (1-3) và class classify (11-12-Ôn thi đại học).

✅ 10. Nút learning -> dropdown:
   -> Roadmap Tutor: có trang giao tiếp với AI, lấy roadmap đã được phân/tạo của user để trao đổi.
   -> Học cùng AI: có trang riêng hiển thị "Tính năng đang được update, các bạn vui lòng đón chờ nhé!".

✅ 11. AI có khung roadmap chính theo Level + phân loại lớp:
   API ai-roadmap trả thêm frameworkCode/frameworkTitle/frameworkDescription/frameworkModules;
   random mini-test và final-test theo skill plan bám level + academicTrack + Question Bank.

✅ 12. Thiết kế trang roadmap:
   html đã chuyển từ tĩnh 3 module sang flow 5 module;
   module 1 luôn mở học thử, module 2+ khóa theo điều kiện đăng ký + tiến độ module trước;
   AI render giáo trình/chuyên đề theo level + lớp của user.

✅ 13. Roadmap theo tiến độ:
   roadmap 5 module chạy theo tiến độ thực tế;
   cứ hoàn thành mỗi 2 module sẽ bật checkpoint check tiến độ.

✅ 14. Tiến độ user < tiến độ roadmap:
   tại checkpoint có hỏi user có theo kịp không;
   có -> học tiếp;
   không -> gọi API replanning để điều chỉnh tiến độ chậm hơn.

✅ 15. Tiến độ user > tiến độ roadmap:
   hệ thống nhận diện user học nhanh hơn (eligible up-level);
   hỏi user có muốn up-level không;
   có -> làm bài thi up-level và cập nhật level theo kết quả;
   không -> học tiếp level hiện tại.

✅ 16. Cần hướng dẫn setup AI (lấy API từ chatgpt), để AI web có thể hoạt động
   đã có file `AI_SETUP.md` hướng dẫn setup OpenAI API key;
   đã map biến môi trường `OPENAI_API_KEY` trong `docker-compose.yml`.

✅ 17. Admin: xóa cả 2 màn content bank và roadmap config
   đã xóa menu/sidebar của 2 màn này ở admin dashboard + question bank;
   đã gỡ action binding tương ứng trong `FE/js/features/admin.js`.

✅ 18. Admin: thêm màn Question Bank (CRUD câu hỏi hoặc dùng json để thêm đề, môn toán dùng Latex để viết công thức toán học).
   đã hoàn thiện CRUD Question Bank;
   thêm import chuỗi JSON theo schema legacy (ID, Subject_Code, Level, Skill_Tag, Question_Text, Option_A..D, Correct, Class, Classify);
   hỗ trợ map môn học alias (M/V/E...) + giữ nguyên chuỗi LaTeX cho môn Toán;
   bổ sung phân loại OTDH (UNI_PREP) trong filter/form/import.

✅ 19. mapping sao cho AI có thể lấy đề và phân loại đúng cho từng user
   đã map flow placement theo `gradeBand` + `gradeLevel` + `subject` để lấy đề đúng từ Question Bank;
   đã cập nhật generate placement paper mix level (L1/L2/L3) và fallback an toàn khi thiếu dữ liệu.

✅ 20. AI cần phải tạo roadmap sao cho phù hợp với từng user, roadmap chính là phần được chia ra thành các module, mỗi module sẽ có giáo trình/chuyên đề ở dưới.
   đã trả thêm `roadmapModules` cá nhân hóa từ AI (moduleNo/title/focusSkills/studyGuide/targetScore/duration);
   frontend roadmap đã render nội dung module theo dữ liệu thật thay vì template tĩnh.

✅ 21. Bảng Recent student signups thì cần data thật nhưng chỉ cần hiển thị Tên, Lớp và Date thôi
   đã render bảng Recent Student Signups bằng dữ liệu thật từ `/api/admin/users`;
   chỉ giữ đúng 3 cột Tên, Lớp (academicTrack), Date (createdAt).

✅ 22. Dashboard overview thay phần hiển thị Student Progress Trends thành phần Tracking lượng user truy cập vào web (k cần đăng kí hay đăng nhập) và user đăng kí mới, biểu đồ cột giữ nguyên nhưng cần data thật (theo lượng truy cập).
   đã thêm tracking truy cập web qua `/api/analytics/visit` (không cần login, 1 visitor/ngày);
   admin dashboard đã hiển thị biểu đồ cột dữ liệu thật 7 ngày: `visits` và `newUsers`.

✅ 23. vì chúng ta yêu cầu phải có kết quả placement test trước thì mới được vào học roadmap nên là khi user nhấn vào continue journey thì phải check xem đã có kết quả (được phân loại level) chưa:
   có -> vào roadmap để học như bình thường;
   không -> popup thông báo user cần phải làm placement test trước.
